# CFBBS 论坛 · 部署与维护说明（中文版）

> 这是你自己的论坛项目。改动代码后推送到 GitHub 的 `main` 分支，会自动部署到 Cloudflare（不用手动操作）。

---

## 一、怎么重新部署

**改完代码（或改完配置）后**，把改动提交并推送到 GitHub `main` 分支即可，GitHub 会自动跑部署，大约 1~2 分钟生效。

查看部署是否成功：GitHub 仓库 → **Actions** 页面，最新一条显示绿色对勾 = 成功。

---

## 二、部署依赖的配置（GitHub Actions 里的"秘密"）

位置：GitHub → `daihuan0612/CFBBS` → **Settings** → **Secrets and variables** → **Actions**

这些是部署时用到的钥匙，都填在"New repository secret"里（名称必须一字不差）：

| 名称 | 用途 | 说明 |
|---|---|---|
| `CF_API_TOKEN` | Cloudflare 的钥匙 | 部署时用它登录你的 Cloudflare 账号 |
| `CF_ACCOUNT_ID` | Cloudflare 账号 ID | 账号主页能看到 |
| `JWT_SECRET` | 论坛登录加密串 | 必须 ≥32 位，随便编一串长乱码 |
| `ADMIN_EMAIL` | 管理员邮箱 | 首次部署自动建管理员 |
| `ADMIN_PASSWORD` | 管理员密码 | 首次部署自动建管理员 |
| `ADMIN_NICKNAME` | 管理员昵称 | 可留空 |
| `IMGBED_AUTH_CODE` | **图床（网盘）上传密码** | 论坛上传图片/视频时用，必须和图床那边一致 |
| `BASE_URL` | 可留空 | 不用管 |

> 这些"秘密"是加密存储的，仓库里、日志里都看不到，不会被公开。

---

## 三、以后想改图床上传密码（重要）

上传密码存在 **GitHub Actions secrets** 里（不直接在代码里，更安全、不会丢）。

改法：

1. 打开 GitHub → `daihuan0612/CFBBS` → **Settings** → **Secrets and variables** → **Actions**
2. 点 `IMGBED_AUTH_CODE` 那一行 → **Update**
3. 填新密码 → 保存
4. 回到仓库，随便改动一下（比如改一行注释）并提交，让部署跑一次
5. 部署完成后，新密码生效

> 注意：**图床那边**（CloudFlare-ImgBed）的上传密码也要改成同一个，两边必须一致才能传。

---

## 四、和上传有关的环境变量

论坛上传走的链路是：浏览器 → 直接传到你的图床（网盘）→ 再回论坛登记。

- `IMGBED_DOMAIN`：图床网址（如 `https://yun.siyou.qzz.io`），在 Cloudflare Worker 后台设置
- `IMGBED_AUTH_CODE`：图床的上传密码（存在 GitHub secrets，见上）
- `IMGBED_ADMIN_TOKEN`：图床的管理 Token（删除帖子时自动删图用，可选）

> 论坛上传限制：**只允许传到图床的 `tucao` 文件夹**，**只走 Telegram 渠道**（这些已写死在代码里）。

---

## 五、遇到"上传不了"先查这几样

1. 部署是否成功（GitHub → Actions 看最新一条）
2. `IMGBED_AUTH_CODE` 是否在 GitHub secrets 里（见上第三节）
3. `IMGBED_DOMAIN` 是否在 Cloudflare Worker 后台设置好了（必须和你图床域名**完全一致**，不要带多余斜杠）
4. 图床那边的上传密码是否和 `IMGBED_AUTH_CODE` 一样
5. 用浏览器开发者工具（F12）→ 网络，看上传请求返回什么错误

> 提示"仅允许登记本图床 tucao 目录下的文件"：这是论坛在检查文件是否确实传到了 tucao 目录（防滥用）。已经修复了图床地址带 `/file/` 前缀的情况，正常不会再出现；如果还出现，多半是上面第 3 条——`IMGBED_DOMAIN` 和图床实际域名对不上。

---

## 六、常见问题

**Q：部署失败了，提示 secret 相关错误？**
A：之前出现过"最新版本未部署"导致的失败，已修复（改用 `versions secret put`）。如果还失败，去 Cloudflare 后台 → Workers → `cfbbs`，把"未部署的新版本"点部署或删掉，再重新部署一次。

**Q：改了代码但没生效？**
A：确认 push 到了 `main` 分支，且 Actions 显示成功。Cloudflare 有 CDN 缓存，等 1~2 分钟或强制刷新浏览器。

**Q：密码忘了？**
A：在 GitHub → Settings → Secrets and variables → Actions 里可以重新填（改完记得触发一次部署）。

---

*说明：本项目部署自动流程见 `.github/workflows/deploy.yml`（英文，不必看，上面已翻译成人话）。*
