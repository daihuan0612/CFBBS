# CFBBS 部署说明（公开仓库版）

> 本文可提交到公开仓库：不包含任何真实域名、账户 ID、密码或 Token。生产环境请使用自己的值替换所有 `example.com`、`<...>` 占位符。

## 1. 架构：为什么有两个同名项目

CFBBS 正常部署会使用两个 Cloudflare 项目：

1. **Worker `cfbbs`**：后端 API、登录、D1 数据库、R2 文件、图床上传代理、定时清理。
2. **Pages `cfbbs`**：前端静态网页。

它们名称相同但类型不同，二者都需要保留：删除 Worker 会使 API/登录失效；删除 Pages 会使网页打不开。

推荐域名拆分：

```text
论坛网页：https://forum.example.com       → Pages 项目 cfbbs
后端 API：https://api.example.com         → Worker cfbbs
图床：    https://img.example.com          → 独立图床项目
```

直接访问 `https://api.example.com/` 返回 `Not Found` 是正常的；它是 API 服务。请使用 `https://api.example.com/api/config` 检查 API 是否正常。

## 2. Worker 环境变量：填什么、放哪里

位置：Cloudflare Dashboard → **Workers & Pages** → **cfbbs** → **Settings** → **Variables and Secrets**。

### Secret（加密变量）

下列值不要写入代码、`wrangler.jsonc` 或公开文档。

| 名称 | 必需 | 填什么 |
|---|---|---|
| `JWT_SECRET` | 是 | 至少 32 字符的随机长字符串，用于登录令牌签名。生产环境不要随意修改，修改后用户需要重新登录。 |
| `IMGBED_AUTH_CODE` | 图床上传时是 | 图床端配置的上传密码。必须和图床端完全一致。 |
| `ADMIN_EMAIL` | 否 | 用于初始化管理员的邮箱。 |
| `ADMIN_PASSWORD` | 否 | 初始化管理员密码；仅与 `ADMIN_EMAIL` 一起填写才有意义。 |
| `ADMIN_NICKNAME` | 否 | 初始化管理员显示名称；不填时默认 `Admin`。 |
| `BASE_URL` | 推荐 | API 根地址，例如 `https://api.example.com`。 |
| `R2_PUBLIC_BASE_URL` | 推荐 | R2 文件访问前缀，例如 `https://api.example.com/r2`。 |
| `IMGBED_ADMIN_TOKEN` | 否 | 图床管理 API Token；配置后，删帖可尝试同步删图床文件。 |
| `TURNSTILE_SECRET_KEY` | 启用人机验证时 | Cloudflare Turnstile 的 Secret key。 |

### 普通变量

| 名称 | 填什么 |
|---|---|
| `IMGBED_DOMAIN` | 图床完整地址，例如 `https://img.example.com`；不要以 `/` 结尾。 |
| `TURNSTILE_SITE_KEY` | 启用 Turnstile 时填写站点 Site key。 |

## 3. 图床上传说明

上传链路为：

```text
浏览器 → CFBBS Worker /api/media/upload → 图床 tucao 目录（Telegram 渠道）
```

浏览器不会获得 `IMGBED_AUTH_CODE`；只有 Worker 服务端持有它。

支持的文件：

- 图片：JPG、PNG、GIF、WebP、BMP，最大 10 MB；**SVG 被禁用**。
- 视频：MP4、WebM、MOV、AVI，最大 500 MB。
- 压缩包：ZIP、RAR、7z、tar、gz、tgz，最大 300 MB。

常见故障：

| 提示 | 原因与处理 |
|---|---|
| `图床上传失败 (530)` | 图床域名自身返回 Cloudflare 530/1016；先修图床 DNS/自定义域名。 |
| `上传服务未配置` | Worker 缺少 `IMGBED_DOMAIN` 或 `IMGBED_AUTH_CODE`。 |
| `不支持的文件类型` | 文件不是支持的图片/视频/压缩包，或扩展名不匹配；SVG 不允许上传。 |
| 缩略图晚一点才出现 | 前端会异步查询媒体信息并加载图片；首次访问或缓存更新后可能稍慢。 |

## 4. 自定义域名与 DNS

### 前端域名

将论坛主域名绑定到 Pages 项目 `cfbbs`，并添加明确 DNS 记录：

```text
Type: CNAME
Name: forum
Target: cfbbs.pages.dev
Proxy status: Proxied
TTL: Auto
```

如果 Pages 域名状态一直为 `pending` 且显示 `CNAME record not set`，说明 DNS 记录未生效。即使有 `*` 通配符记录，也应单独创建 `forum` 的 CNAME。

### API 域名

将 `api.example.com` 作为 **Worker `cfbbs` 的 Custom Domain / Route** 绑定。不要把 API 域名指向 `cfbbs.pages.dev`。

### 图床域名

图床域名属于图床项目，不属于论坛 Worker/Pages。若图床地址返回 530/1016，应修图床项目自己的 DNS/域名绑定。

## 5. 本地直接部署到 Cloudflare

本项目可不依赖 GitHub Actions，使用 Wrangler 直接部署。

```bash
# 1. 安装依赖
npm ci

# 2. 通过浏览器登录 Cloudflare
npx wrangler login

# 3. 仅在新增 migration 时执行数据库迁移
npx wrangler d1 migrations apply cforum-db --remote

# 4. 部署 Worker
npx wrangler deploy
```

前端构建文件中的 API 地址占位符是 `__WORKER_URL__`。部署 Pages 前，构建产物必须注入真实 API 地址；不要把替换后的源码提交到 Git：

```powershell
$apiFile = 'frontend\src\lib\api.ts'
$original = Get-Content -LiteralPath $apiFile -Raw
try {
  $built = $original.Replace("const WORKER_URL = '__WORKER_URL__';", "const WORKER_URL = 'https://api.example.com';")
  [System.IO.File]::WriteAllText((Join-Path (Get-Location) $apiFile), $built, [System.Text.UTF8Encoding]::new($false))
  npm run build:frontend
  npx wrangler pages deploy public --project-name=cfbbs --branch=master
} finally {
  [System.IO.File]::WriteAllText((Join-Path (Get-Location) $apiFile), $original, [System.Text.UTF8Encoding]::new($false))
}
```

同时修改前后端时，先部署 Worker，再构建并部署 Pages。

## 6. GitHub Actions（可选备用方式）

工作流位于 `.github/workflows/deploy.yml`，默认在推送 `master` 时触发。使用前，在 GitHub 仓库 → **Settings** → **Secrets and variables** → **Actions** 配置 Secrets：

| GitHub Secret | 填什么 |
|---|---|
| `CF_API_TOKEN` | Cloudflare 部署 API Token，最小权限应覆盖 Workers、D1、R2、Pages 编辑和账户读取。 |
| `CF_ACCOUNT_ID` | 你的 Cloudflare Account ID。 |
| `JWT_SECRET` | 与 Worker 使用的 JWT Secret 相同。 |
| `BASE_URL` | 例如 `https://api.example.com`。 |
| `WORKER_URL` | 例如 `https://api.example.com`，供前端构建使用。 |
| `ADMIN_EMAIL` / `ADMIN_PASSWORD` / `ADMIN_NICKNAME` | 可选管理员初始化信息。 |
| `IMGBED_AUTH_CODE` | 图床上传密码。 |
| `TURNSTILE_SITE_KEY` / `TURNSTILE_SECRET_KEY` | 可选 Turnstile 配置。 |

`CF_API_TOKEN` 是 Cloudflare Token，不是 GitHub PAT；所有敏感值都必须添加为 **Secrets**，不要添加为普通 Variables。

若不使用 GitHub Actions，请在 GitHub Actions 页面 Disable workflow，或修改工作流触发条件。仅删除 Secret 会让工作流每次推送都失败。

## 7. 安全与维护提醒

- 不要将 GitHub PAT、Cloudflare API Token、图床密码、JWT Secret 提交到仓库或粘贴到公开渠道。
- 曾经泄露过的 Token 应立即在相应控制台撤销，改用新的最小权限 Token。
- 不要删除 D1、R2、Worker 或 Pages 项目；部署 Worker/Pages 本身不会清空用户、帖子或文件。
- Worker 每天会运行一次清理任务，删除过期会话、临时密码和限流记录，这是正常行为。
